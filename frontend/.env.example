# URL of the deployed FastAPI backend (Phase 2-4). Leave unset to run the
# frontend standalone in local-demo mode with mock analysis.
# Example: VITE_API_URL=https://permissionlens-api.onrender.com
VITE_API_URL=